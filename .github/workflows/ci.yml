name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Node ${{ matrix.node }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: npm
          cache-dependency-path: server/package-lock.json

      - name: Install MCP server dependencies
        run: npm ci --prefix server

      # The smoke client resolves the SDK from server/node_modules, so the
      # repository test suite remains valid without a second root install.

      - name: Run offline test suite
        run: npm test
        env:
          OPENROUTER_API_KEY: ""

      - name: Verify execution foundation
        run: node test/execution.mjs

      - name: Validate bundled role packs
        run: >-
          node --input-type=module -e
          "import fs from 'node:fs'; import { loadRolePack } from './core/role-packs.mjs';
          for (const file of fs.readdirSync('role-packs'))
          loadRolePack('role-packs/' + file, { root: 'role-packs' });"

      - name: Audit release contents and branding
        run: npm run audit:release
